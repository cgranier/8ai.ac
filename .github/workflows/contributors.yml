name: Update Contributors

on:
  push:
    branches:
      - main

jobs:
  contrib-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - run: mkdir -p build

      - name: Generate contributors (avatars only)
        id: contributors
        uses: jaywcjlove/github-action-contributors@main
        with:
          output: build/CONTRIBUTORS.svg
          avatarSize: "36"

      - name: Update README with contributors
        uses: jaywcjlove/github-action-modify-file-content@main
        with:
          path: README.md
          body: |
            <div style="display:flex;flex-wrap:wrap;gap:6px;align-items:center">${{ steps.contributors.outputs.htmlList }}</div>
          openDelimiter: "<!-- readme: contributors -start -->"
          closeDelimiter: "<!-- readme: contributors -end -->"
          message: "docs(contributor): update contributors in README"
